<form method="post" action="{{ path('app_parcelle_proprietes_delete', {'id': parcelle_propriete.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parcelle_propriete.id) }}">
    <button class="btn">Delete</button>
</form>
