{% extends 'base_back.html.twig' %}

{% block title %}Ajouter un Matériel{% endblock %}

{% block body %}

<div class="container mt-5 pt-5" style="margin-left: 250px;">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg">
                <div class="card-header bg-primary text-white text-center">
                    <h2>Ajouter un Matériel Agricole</h2>
                </div>
                <div class="card-body">
                    {{ include('materielvente/_form.html.twig', { 'button_label': 'Enregistrer' }) }}
                </div>
            </div>

            <div class="text-center mt-4">
                <a href="{{ path('app_tables') }}" class="btn btn-outline-secondary">
                    <i class="fas fa-arrow-left"></i> Retour à la liste
                </a>
            </div>
        </div>
    </div>
</div>

{% endblock %}
