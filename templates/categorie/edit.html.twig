{% extends 'base.html.twig' %}

{% block title %}Edit Categorie{% endblock %}

{% block body %}


<div class="container mt-5 pt-5">

    <h1 class="text-center">Edit Categorie</h1>
{{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class': 'container mt-4 p-4 bg-light rounded shadow'}}) }}

    <div class="card p-4 mt-3">
        {{ include('categorie/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
<div class="d-flex justify-content-center mt-4">
            <a href="{{ path('app_categorie') }}" class="btn btn-primary me-3">
                <i class="fa fa-arrow-left"></i> Retour
            </a>
           
        </div>

    
    <div class="text-center mt-3">
        {{ include('categorie/delete.html.twig') }}
    </div>
</div>
{{ form_end(form) }} 
     
{% endblock %}
